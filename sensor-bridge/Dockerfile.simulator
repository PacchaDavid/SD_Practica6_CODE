FROM python:3.11-slim
WORKDIR /app
COPY sensor-bridge/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY sensor-bridge/sensor_simulador.py ./
ENV MQTT_BROKER=mosquitto MQTT_PORT=1883
CMD ["python", "sensor_simulador.py"]
